server {
  listen 3000;
  server_name localhost;
  root /usr/share/nginx/html;
  index index.html index.htm;

  # Desativa a porta nos redirecionamentos do Nginx
  port_in_redirect off;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
